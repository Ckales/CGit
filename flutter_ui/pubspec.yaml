name: cgit_flutter
description: CGit 的 Flutter Desktop UI —— 与 src/ 下的 vanilla JS + WebView 实现做对照
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  cgit_rust:
    path: rust_builder
  flutter:
    sdk: flutter
  flutter_rust_bridge: 2.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
